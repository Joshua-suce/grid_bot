# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# State (contains order IDs and session data)
state/*.json
state/*.json.old
state/*.json.*
state/*.corrupt*
state/*.bak.*
state/*.empty
# atomic-write scratch files; a crash mid-save can leave one behind
state/.state_*.tmp
state/*.tmp

# Cached OHLCV for backtests (regenerable via backtest.fetch_ohlcv)
data/

# pytest tmp_path scratch (see pyproject addopts)
.pytest_tmp/

# Logs (contains trade history and events)
logs/*.csv
logs/*.csv.*
logs/*.jsonl
logs/*.jsonl.*
logs/*.log
logs/*.log.*

# IDE / OS
.vscode/
.idea/
.claude/
*.swp
.DS_Store
Thumbs.db

# Docker
.docker/
logs/klines/
